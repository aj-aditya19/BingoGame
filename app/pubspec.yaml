name: app
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  # Core
  cupertino_icons: ^1.0.8
  flutter_localizations:
    sdk: flutter

  # Networking & State
  http: ^1.2.0
  provider: ^6.1.5
  fluttertoast: ^8.2.1

  # Firebase
  # (removed firebase for this app - using backend MongoDB)

  # Real-time
  socket_io_client: ^3.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
